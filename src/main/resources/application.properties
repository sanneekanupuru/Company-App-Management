# ---------- MySQL datasource (local) ----------
spring.datasource.url=jdbc:mysql://localhost:3306/companydb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sannee.K#564
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ---------- JPA / Hibernate ----------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Make sure data.sql runs after Hibernate has created schema
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# ---------- Logging for SQL parameters ----------
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Optional: change server port if 8080 is busy
# server.port=8081
